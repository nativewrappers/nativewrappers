[@nativewrappers/fivem](../../../../README.md) / [redm/definitions/redm](../README.md) / SetFlyThroughWindscreenParams

# Function: SetFlyThroughWindscreenParams()

## Call Signature

```ts
function SetFlyThroughWindscreenParams(
   vehMinSpeed, 
   unkMinSpeed, 
   unkModifier, 
   minDamage): boolean;
```

Defined in: [src/common-game/definitions/redm.d.ts:22040](https://github.com/nativewrappers/nativewrappers/blob/dc9e2638d1087a48984d9b0f544b0f441166f8eb/src/common-game/definitions/redm.d.ts#L22040)

Sets some in-game parameters which is used for checks is ped needs to fly through windscreen after a crash.

### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `vehMinSpeed` | `number` | Vehicle minimum speed (default 35.0). |
| `unkMinSpeed` | `number` | Unknown minimum speed (default 40.0). |
| `unkModifier` | `number` | Unknown modifier (default 17.0). |
| `minDamage` | `number` | Minimum damage (default 2000.0). |

### Returns

`boolean`

A bool indicating if parameters was set successfully.

## Call Signature

```ts
function SetFlyThroughWindscreenParams(
   vehMinSpeed, 
   unkMinSpeed, 
   unkModifier, 
   minDamage): boolean;
```

Defined in: [src/redm/definitions/redm.d.ts:22040](https://github.com/nativewrappers/nativewrappers/blob/dc9e2638d1087a48984d9b0f544b0f441166f8eb/src/redm/definitions/redm.d.ts#L22040)

Sets some in-game parameters which is used for checks is ped needs to fly through windscreen after a crash.

### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `vehMinSpeed` | `number` | Vehicle minimum speed (default 35.0). |
| `unkMinSpeed` | `number` | Unknown minimum speed (default 40.0). |
| `unkModifier` | `number` | Unknown modifier (default 17.0). |
| `minDamage` | `number` | Minimum damage (default 2000.0). |

### Returns

`boolean`

A bool indicating if parameters was set successfully.
