[@nativewrappers/fivem](../../../../README.md) / [redm/definitions/redm](../README.md) / IsBigmapActive

# Function: IsBigmapActive()

## Call Signature

```ts
function IsBigmapActive(): boolean;
```

Defined in: [src/common-game/definitions/redm.d.ts:5073](https://github.com/nativewrappers/nativewrappers/blob/8fd2e00d7e048b9268f6c990b83eef252e4d6150/src/common-game/definitions/redm.d.ts#L5073)

<!-- Native implemented by Disquse. 0xFFF65C63 -->
Returns true if the minimap is currently expanded. False if it's the normal minimap state.
Use [`IsBigmapFull`](#\_0x66EE14B2) to check if the full map is currently revealed on the minimap.

### Returns

`boolean`

A bool indicating if the minimap is currently expanded or normal state.

## Call Signature

```ts
function IsBigmapActive(): boolean;
```

Defined in: [src/common-game/definitions/redm.d.ts:5081](https://github.com/nativewrappers/nativewrappers/blob/8fd2e00d7e048b9268f6c990b83eef252e4d6150/src/common-game/definitions/redm.d.ts#L5081)

<!-- Native implemented by Disquse. 0xFFF65C63 -->
Returns true if the minimap is currently expanded. False if it's the normal minimap state.
Use [`IsBigmapFull`](#_0x66EE14B2) to check if the full map is currently revealed on the minimap.

### Returns

`boolean`

A bool indicating if the minimap is currently expanded or normal state.

## Call Signature

```ts
function IsBigmapActive(): boolean;
```

Defined in: [src/redm/definitions/redm.d.ts:5073](https://github.com/nativewrappers/nativewrappers/blob/8fd2e00d7e048b9268f6c990b83eef252e4d6150/src/redm/definitions/redm.d.ts#L5073)

<!-- Native implemented by Disquse. 0xFFF65C63 -->
Returns true if the minimap is currently expanded. False if it's the normal minimap state.
Use [`IsBigmapFull`](#\_0x66EE14B2) to check if the full map is currently revealed on the minimap.

### Returns

`boolean`

A bool indicating if the minimap is currently expanded or normal state.

## Call Signature

```ts
function IsBigmapActive(): boolean;
```

Defined in: [src/redm/definitions/redm.d.ts:5081](https://github.com/nativewrappers/nativewrappers/blob/8fd2e00d7e048b9268f6c990b83eef252e4d6150/src/redm/definitions/redm.d.ts#L5081)

<!-- Native implemented by Disquse. 0xFFF65C63 -->
Returns true if the minimap is currently expanded. False if it's the normal minimap state.
Use [`IsBigmapFull`](#_0x66EE14B2) to check if the full map is currently revealed on the minimap.

### Returns

`boolean`

A bool indicating if the minimap is currently expanded or normal state.
