[@nativewrappers/fivem](../../../../README.md) / [redm/definitions/redm](../README.md) / GetCurrentServerEndpoint

# Function: GetCurrentServerEndpoint()

## Call Signature

```ts
function GetCurrentServerEndpoint(): string;
```

Defined in: [src/common-game/definitions/redm.d.ts:2789](https://github.com/nativewrappers/nativewrappers/blob/8fd2e00d7e048b9268f6c990b83eef252e4d6150/src/common-game/definitions/redm.d.ts#L2789)

Returns the peer address of the remote game server that the user is currently connected to.

### Returns

`string`

The peer address of the game server (e.g. `127.0.0.1:30120`), or NULL if not available.

## Call Signature

```ts
function GetCurrentServerEndpoint(): string;
```

Defined in: [src/common-game/definitions/redm.d.ts:2795](https://github.com/nativewrappers/nativewrappers/blob/8fd2e00d7e048b9268f6c990b83eef252e4d6150/src/common-game/definitions/redm.d.ts#L2795)

Returns the peer address of the remote game server that the user is currently connected to.

### Returns

`string`

The peer address of the game server (e.g. `127.0.0.1:30120`), or NULL if not available.

## Call Signature

```ts
function GetCurrentServerEndpoint(): string;
```

Defined in: [src/redm/definitions/redm.d.ts:2789](https://github.com/nativewrappers/nativewrappers/blob/8fd2e00d7e048b9268f6c990b83eef252e4d6150/src/redm/definitions/redm.d.ts#L2789)

Returns the peer address of the remote game server that the user is currently connected to.

### Returns

`string`

The peer address of the game server (e.g. `127.0.0.1:30120`), or NULL if not available.

## Call Signature

```ts
function GetCurrentServerEndpoint(): string;
```

Defined in: [src/redm/definitions/redm.d.ts:2795](https://github.com/nativewrappers/nativewrappers/blob/8fd2e00d7e048b9268f6c990b83eef252e4d6150/src/redm/definitions/redm.d.ts#L2795)

Returns the peer address of the remote game server that the user is currently connected to.

### Returns

`string`

The peer address of the game server (e.g. `127.0.0.1:30120`), or NULL if not available.
